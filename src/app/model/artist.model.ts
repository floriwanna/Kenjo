export class Artist {
    // required: true
    _id: string;

    // required: true
    name: string;

    photoUrl: string
    birthdate: Date;
    deathDate: Date;
    _createdAt: string;
    _updatedAt: string;
}